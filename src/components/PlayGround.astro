<fieldset class="flex-container" id="playGround">
    <legend>Preview</legend>
    <div class="flex-item | bg-color-box-1 color-neutral-900">1</div>
    <div class="flex-item | bg-color-box-2">2</div>
    <div class="flex-item | bg-color-box-3">3</div>
    <div class="flex-item | bg-color-box-4">4</div>
    <span class="main-axis" data-show="false"></span>
    <!-- <span class="cross-axis" data-show="false"></span> -->
</fieldset>

<style lang="scss" >
    .flex-container{
        --justify-content: flex-start;
        --flex-direction : row;
        --align-items: stretch;

        position: relative;
        display: flex;
        flex-direction: var(--flex-direction);
        padding-inline: 0;
        padding-block: 0;
        justify-content: var(--justify-content);
        align-items: var(--align-items);
        min-height: 75svh;
        height: 80svh;

        > .main-axis{
            
            display: none;
            position: absolute;
            // -->
            top: 50%;
            left: 50%;
            width: 70vmin;
            height: 0;
            transform: translate(-50%, -50%) rotate(0deg);
            transition: transform .4s;
            border-bottom: 7px dashed orange;

            // abajo

            


            &[data-show="true"]{
                display: block;
            }

            &.column{
                
                transform: translate(-50%, -50%) rotate(90deg) ;
                // width: 0;
                // height: 70%;
                // border-bottom: none;
                border-left: 7px dashed orange;
            }

            &::after{
                content: "";
                position: absolute;
                background-color: orange;
                right: 0;
                width: 25px;
                aspect-ratio: 1;
                // transform: translate(50%, -25%);
                clip-path: polygon(50% 0%, 0% 100%, 100% 100%);
                transform: translate(60%, -30%) rotate(90deg) ;

            }

            
        }
        
        > .flex-item{
            min-width: min(15vw,90px);
            min-height: min(15vw,90px);
            display: grid;
            place-items: center;
        }

    }
</style>